# Vendored CDX Studio API templates

Source graphs used by `scripts/port-cdx-templates.mjs` to emit
`public/workflows/cdx_*.json`.

Keep this folder in-repo so the port script can be re-run without an
external CDX Studio checkout.
